global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    cluster: 'aod-prod'

rule_files:
  - 'alerts.yml'

alerting:
  alertmanagers:
    - static_configs:
        - targets: ['alertmanager:9093']

scrape_configs:
  # === EC2에 떠 있는 AOD 백엔드 ===
  - job_name: 'aod-ec2-app'
    metrics_path: '/actuator/prometheus'
    scheme: 'https'                        # 🔥 https로 도메인 접근
    static_configs:
      - targets: ['api.allofdophamin.com'] # 포트는 443 기본
        labels:
          application: 'aod-app'
          environment: 'prod'

  # === Prometheus 자기 자신 ===
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']
